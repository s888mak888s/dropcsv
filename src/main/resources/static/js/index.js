/**
 * 
 */
(function (window, undefined) {
    "use strict";

    // documentオブジェクトを使わない場合は不要
    var document = window.document;

    var $ = require("jquery"),
    	Dropzone = require("dropzone");
    $(function ($) {
        // readyハンドラ
//    	var myDropzone = new Dropzone("#drop", { url: "/upload"});
    });
})(window);